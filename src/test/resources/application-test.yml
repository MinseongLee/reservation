spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reservationtest
    username: root
    password: 1111


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true # table info in console
    show-sql: true # table info in console

# value in table
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: TRACE