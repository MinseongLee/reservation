server:
  port: 8080
  servlet:
    session:
      timeout: 5h

spring:
  datasource:
    # ?autoReconnect=true
    url: jdbc:mysql://localhost:3306/reservation
    username: root
    password: 1111
#    dbcp2:
  # true일 경우 커넥션 풀에서 커넥션을 가져올 때 커넥션이 유효한지의 여부를 검사한다.
#      test-on-borrow: true
  # query
#      validation-query: SELECT 1


  jpa:
    hibernate:
      # validation - 운영시 : entity들과 db 내용과 맞는지 검증
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true # table info in console
    show-sql: true # table info in console
#    database-platform: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    # the proejct email!
    username: dexlee345@gmail.com
    # after 2단계인증, 발급 받은 app password
    password: zqnazeaeymknnsrw
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true

# value in table
logging:
  level:
    org:
      hibernate:
#        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE